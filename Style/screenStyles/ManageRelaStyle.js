import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexGrow: 1,
        backgroundColor: '#FFF1E7',
        alignItems: 'center',
    },
    actionsContainer: {
        width: '100%',
        alignItems: 'center',
    },
})
